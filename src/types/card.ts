export type Product = {
  title: string;
  content: string;
  url: string;
};
export type Feedback = {
  logo: string;
  content: string;
  avatar: string;
  name: string;
  position: string;
};

export type Event = {
  title: string;
  text: string;
  url: string;
};

export type Article = {
  title: string;
  content: string;
  url: string;
};

export type Collection = {
  icon: any;
  text: string;
};
